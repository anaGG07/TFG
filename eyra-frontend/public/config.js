// Este archivo de configuración se sirve como un script público
// y se carga antes que la aplicación principal
window.RUNTIME_CONFIG = {
  API_URL: "https://old.eyraclub.es/api"
};

console.log("🔧 Configuración en tiempo de ejecución cargada:", window.RUNTIME_CONFIG);
